<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- 1. 배경을 위에서 만든 그라데이션 Drawable로 설정 -->
        <item name="windowSplashScreenBackground">@color/pink</item>

        <!-- 2. 아이콘은 흰색 로고만 있는 Vector Drawable 또는 PNG 사용 -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_logo_no_background</item>
        <!-- 선택: 아이콘 애니메이션 후 테마 -->
        <item name="postSplashScreenTheme">@style/Theme.YogaYo</item>

        <!-- 선택: 아이콘 배경색 (아이콘이 컨테이너를 꽉 채우지 않을 경우 보임) -->
<!--         <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>-->

        <!-- 선택: 애니메이션 지속 시간 -->
<!--         <item name="windowSplashScreenAnimationDuration">1000</item>-->
    </style>
</resources>